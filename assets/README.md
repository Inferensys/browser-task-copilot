# Assets

Add screenshots or recordings that match replay artifacts and docs.

Recommended files:

- `browser-run-01.png`: task opened and initial navigation complete
- `browser-run-02.png`: approval gate reached for write action
- `browser-run-03.png`: post-approval execution state
- `replay-timeline.png`: replay UI or terminal timeline view

Guidelines:

- redact tenant-identifying data
- keep resolution at 1440x900 for consistency
- include UTC timestamp in image metadata when possible
